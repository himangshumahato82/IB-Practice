import React from 'react';
import { useState,useRef } from 'react';
function Timer(props) {

    
  
    return (
      <div>
        <h1></h1>
        <button>Start</button>
        <button>Stop</button>
        <button>Pause</button>
      </div>
    );
}

export default Timer;